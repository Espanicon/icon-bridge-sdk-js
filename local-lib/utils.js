"use strict";
const utils = require("../dist/utils/utils");
const { networks, contracts, abiDataPath, labels, tokenLabels, removeZerosFromAddress, getFormattedHostname, sleep } = utils;
const utilsSlim = {
    networks,
    contracts,
    abiDataPath,
    labels,
    tokenLabels,
    removeZerosFromAddress,
    getFormattedHostname,
    sleep
};
module.exports = utilsSlim;
//# sourceMappingURL=utils.js.map